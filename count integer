#include<stdio.h>
#include<conio.h>
void main()
{
int n,count=0;
printf("\n Enter the integers:");
scanf("%d",&n);
while(n!=0)
{
n=n/10;
++count;
}
printf("\n The number of integers is %d",count);
getch();
}
